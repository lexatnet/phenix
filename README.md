Local Depoyment

requirements:
        - docker-compose

steps:
        ```
        make init
        make deps-install
        ```

Run local
    ```
    make run
    ```

requirements:
        - vagrant

Go to project root

   run
   ```
   vagrant up
   ```
